<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <item name="android:windowBackground">@color/activityBackground</item>
  </style>
  <!-- CloudStreamSettingsActivity: an invisible window over StreamDek, so a plugin's own settings
       dialog or bottom sheet reads as a popup. MaterialComponents because CloudStream extensions
       build those with Material classes and styles, as CloudStream's own theme allows. -->
  <style name="Theme.StreamDek.PluginSettings" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorSecondary">@color/colorPrimary</item>
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:backgroundDimEnabled">false</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
  </style>
  <!-- Everything every platform understands. The v33 copy adds what only Tiramisu has. -->
  <style name="Theme.App.SplashScreen.Base" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
    <!-- The bitmap already carries the padding the platform expects: a 288dp canvas with
         the mark inside the 192dp circle the splash masks to. The old splashscreen_logo_small
         wrapper insetted a layer-list instead, but its bitmap used gravity="center", which
         centres at native size rather than scaling - so a full-bleed logo overflowed the
         inset box and the circular mask sliced the left side off the mark. -->
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.App.SplashScreen.Base"/>
</resources>
